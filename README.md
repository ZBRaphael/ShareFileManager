# ShareFileManager

## 部署和安装

### 客户端

安装app-debug.apk即可

### 服务器端

服务器地址设置为192.168.1.103
端口号5001保证没有被占用

在jar包所在的文件加创建sharefile文件夹

运行一下命令

```shellcode
java -jar socket.jar
```

保证客户端和服务器端在同一个局域网下

